{
  "name": "blindtest-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=apps/mobile",
    "web-host": "npm run dev --workspace=apps/web-host"
  },
  "devDependencies": {
    "@expo/ngrok": "^4.1.3"
  }
}
