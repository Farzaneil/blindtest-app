name: CI

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm install
      - run: npm run lint --workspace=apps/web-host
      - run: npm run lint --workspace=apps/mobile
      - run: npm run typecheck --workspace=apps/web-host
      - run: npm run typecheck --workspace=apps/mobile
